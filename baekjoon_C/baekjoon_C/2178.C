#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int n,m;
	int i;
	int num;
	int maze[100][100] = { 0, };

	scanf("%d %d", &n, &m);

	for (i = 0; i < n; i++)
	{
		scanf("%d", &num);

	}

	return 0;
}